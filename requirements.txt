# Pinned versions — do not use >= to avoid silent breakage between sessions
# Install: pip install -r requirements.txt
numpy==1.24.4
pandas==2.0.3
matplotlib==3.7.5
seaborn==0.13.2
scipy==1.11.4
scikit-learn==1.3.2
scikit-optimize==0.9.0
plotly==5.18.0

# Reinforcement learning
stable-baselines3==2.3.0
gymnasium==0.29.1

# ompl is installed via apt: sudo apt install ros-humble-ompl
# (pip wheel not available for Linux/Ubuntu 22.04)

# ROS2 Python bindings installed via apt (not pip):
#   sudo apt install ros-humble-rclpy ros-humble-gazebo-ros-pkgs \
#     ros-humble-nav2-msgs ros-humble-turtlebot3 ros-humble-turtlebot3-simulations

# Dev / test
pytest==7.4.4
black==23.12.1
flake8==6.1.0
